{
  "name": "nc-events-platform-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd BE && npm install && node app.js",
    "start-frontend": "cd FE && xdg-open index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.5.1"
  }
}
